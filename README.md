# Cookies Among Humans

To install, cd into the repo directory and run

    $ npm install
    $ sudo npm install grunt-cli

To run locally, try

    $ grunt && node app.js

Then open up this: http://localhost:3000
